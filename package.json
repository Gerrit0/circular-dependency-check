{
  "name": "circular-dependency-check-action",
  "version": "2.0.2",
  "main": "action.js",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "madge": "^7.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  },
  "scripts": {
    "lint": "npx prettier --check .",
    "build": "esbuild action.js --bundle --minify --outfile=dist/action.bundle.js --platform=node --target=node20"
  }
}
